{
  "name": "review-panel",
  "version": "0.0.1",
  "description": "Admin Panel for reviews",
  "license": "ISC",
  "author": "",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "prisma generate && prisma migrate deploy && node server.js",
    "dev": "prisma generate && prisma migrate dev --name init && nodemon server.js"
  },
  "dependencies": {
    "@prisma/client": "^6.0.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.1",
    "nodemon": "^3.1.14",
    "resend": "^6.12.2"
  },
  "devDependencies": {
    "prisma": "^6.0.0"
  }
}
